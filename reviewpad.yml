workflows:
  - name: auto-merge-on-required-approvals
    description: Automerge after required approvals if no changes were made critical code
    always-run: true
    if:
      - '!$hasAnnotation("critical") && $hasRequiredApprovals(1, $team("trailblazers"))'
    then:
      - $merge()
